{
  "name": "cwords",
  "module": "scripts/generate-cwords.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "debug": "bun run --inspect-wait scripts/generate-cword.ts",
    "generate": "bun run scripts/generate-cword.ts",
    "init": "bun run scripts/insert-words.ts"
  },
  "dependencies": {
    "@heroicons/react": "^2.1.5"
  }
}